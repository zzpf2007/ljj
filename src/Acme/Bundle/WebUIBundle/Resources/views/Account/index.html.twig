{% extends 'AcmeWebUIBundle::layouts/adminskin.html.twig' %}

{% block content %}

    <div class="am-cf am-padding">
      <div class="am-fl am-cf"><strong class="am-text-primary am-text-lg"> Account </strong> </div>
    </div>

    <div class="am-g">
      <div class="am-u-sm-12">
      
	          <table class="am-table am-table-striped am-table-hover table-main">
		        
		            <tr>
		              <td> </td>
		              <td class="am-hide-sm-only">ID</td>
		              <td class="am-hide-sm-only">UserName</td>
		              <td class="am-hide-sm-only">Name</td>
		              <td class="am-hide-sm-only">Photo</td>
		              <td class="am-hide-sm-only">PhoneNumber</td>
		              <td class="am-hide-sm-only">OtherNumber</td>
		              <td class="am-hide-sm-only">SignAture</td>
		              <td class="am-hide-sm-only">Number</td>
		              <td class="am-hide-sm-only">Balance</td>
		              <td>操作</td>
		            </tr>

                    {% for user in users %}
	                    <tr>
	                      <td><input type="checkbox" /></td>
	                      <td class="am-hide-sm-only">{{ user.id }}</td>
	                      <td class="am-hide-sm-only">{{ user.username }}</td>
			              <td class="am-hide-sm-only">{{ user.account ? user.account.name : '' }}</td>
			              <td class="am-hide-sm-only">{{ user.account ? user.account.photo : ''}}</td>
			              <td class="am-hide-sm-only">{{ user.account ? user.account.phonenumber : '' }}</td>
			              <td class="am-hide-sm-only">{{ user.account ? user.account.othernumber : ''}}</td>
			              <td class="am-hide-sm-only">{{ user.account ? user.account.signature : '' }}</td>
			              <td class="am-hide-sm-only">{{ user.account ? user.account.number : ''}}</td>
			              <td class="am-hide-sm-only">{{ user.account ? user.account.balance : '' }}</td>
			              <td>
			                <div class="am-btn-toolbar">
			                  <div class="am-btn-group am-btn-group-xs">

			                    {#} <a href=" {{ path('category_edit_path', { id : category.id })  }} ">
					                 <button class="am-btn am-btn-default am-btn-xs am-text-secondary"><span class="am-icon-pencil-square-o"></span> 编辑</button>
					             </a>   					               
					             <div class="del">
				                   {{ form_start( delete_form, { 'action': path( 'category_delete_path', { 'id' : category.id } ) } ) }}                    
				                   {{ form_widget( delete_form ) }}  
				                   <input type="submit" value="删除" />
				                   {{ form_end(delete_form) }}
				                 </div>#}
			                  </div>
			                </div>
			              </td>
	                    </tr>
                    {% endfor %}
		    
	          </table>
         
      </div>

    </div>
 
{% endblock %}